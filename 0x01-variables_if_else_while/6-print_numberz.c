#include <stdio.h>

/**
 * main - Entry point. Print Numbers using
 *
 * Return: 0 success
 */
int main(void)
{
	int numbers = 48;

	while (numbers >= 48 && numbers <= 57)
	{
		putchar(numbers);
	}
	putcahr('\n');

	return (0);
}
